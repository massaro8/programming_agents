# Documentation workflow

Select one READY DOCS item. Identify audience → identify authoritative sources → locate documentation surface → make minimum required update → verify links, commands, and examples → complete record and changelog → DONE → sync registry → stop.

Do not change production code unless the work item explicitly requires it.
