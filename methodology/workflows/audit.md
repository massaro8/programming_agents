# Audit Workflow

Run deterministic/static checks first. Review resulting hotspots plus high-risk boundaries. Produce
findings with severity, evidence, recommended action, checks actually executed, and residual uncertainty.
Do not perform opportunistic refactors during the audit.
